{% liquid
  assign menu = section.settings.menu
  assign cart_count = cart.item_count
  if shop.customer_accounts_enabled
    assign account_url = routes.account_url
  else
    assign account_url = routes.account_login_url
  endif
%}

<header class="ccg-header">
  <div class="ccg-header__bar">
    <a href="{{ routes.root_url }}" class="ccg-header__logo">
      {%- if section.settings.logo_image != blank -%}
        {{
          section.settings.logo_image
          | image_url: width: 300
          | image_tag: widths: '120, 180, 240, 300', alt: shop.name, class: 'ccg-header__logo-img'
        }}
      {%- else -%}
        <svg class="ccg-header__logo-icon" viewBox="0 0 64 40" fill="currentColor" aria-hidden="true">
          <path d="M32 4c-3 0-5 2-6 4-1-2-3-3-5-2-2 1-2 4 0 6-3 0-6 2-7 5-3 0-6 2-6 5 0 2 1 3 3 3-1 2 0 4 2 5 0 2 2 4 5 4h28c3 0 5-2 5-4 2-1 3-3 2-5 2 0 3-1 3-3 0-3-3-5-6-5-1-3-4-5-7-5 2-2 2-5 0-6-2-1-4 0-5 2-1-2-3-4-6-4z" />
        </svg>
      {%- endif -%}
      <span class="ccg-header__logo-text">{{ section.settings.logo_text | default: shop.name }}</span>
    </a>

    <nav class="ccg-header__nav" aria-label="Primary">
      {%- for link in menu.links -%}
        <a
          href="{{ link.url }}"
          class="ccg-header__nav-link{% if link.active %} ccg-header__nav-link--active{% endif %}"
        >{{ link.title }}</a>
      {%- endfor -%}
    </nav>

    <div class="ccg-header__actions">
      <a href="{{ routes.search_url }}" class="ccg-header__action" aria-label="Search">
        <svg class="ccg-header__action-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" aria-hidden="true">
          <circle cx="10.5" cy="10.5" r="6.5" />
          <line x1="20" y1="20" x2="15.3" y2="15.3" />
        </svg>
        <span class="ccg-header__action-text">Search</span>
      </a>
      <a href="{{ account_url }}" class="ccg-header__action" aria-label="Account">
        <svg class="ccg-header__action-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" aria-hidden="true">
          <circle cx="12" cy="8" r="4" />
          <path d="M4 20c0-4.4 3.6-7 8-7s8 2.6 8 7" />
        </svg>
        <span class="ccg-header__action-text">Account</span>
      </a>
      <a href="{{ routes.cart_url }}" class="ccg-header__action ccg-header__action--cart" aria-label="Cart ({{ cart_count }})">
        <svg class="ccg-header__action-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" aria-hidden="true">
          <path d="M6 8h12l-1 12H7L6 8Z" />
          <path d="M9 8V6a3 3 0 0 1 6 0v2" />
        </svg>
        {%- if cart_count > 0 -%}
          <span class="ccg-header__cart-count">{{ cart_count }}</span>
        {%- endif -%}
        <span class="ccg-header__action-text">Cart ({{ cart_count }})</span>
      </a>
    </div>

    <details class="ccg-header__mobile-toggle">
      <summary class="ccg-header__mobile-summary" aria-label="Menu">
        <span class="ccg-header__burger"></span>
      </summary>
      <nav class="ccg-header__mobile-nav" aria-label="Primary mobile">
        {%- for link in menu.links -%}
          <a href="{{ link.url }}" class="ccg-header__mobile-link">{{ link.title }}</a>
        {%- endfor -%}
      </nav>
    </details>
  </div>
</header>

{% style %}
  .ccg-header {
    --ccg-ink: #211c17;
    --ccg-cream: #fbf8f2;
    --ccg-tan: #7a6d5d;
    --ccg-header-menu-font-family: 'Barlow Condensed', sans-serif;
    --ccg-header-menu-font-weight: 500;
    --ccg-header-menu-letter-spacing: 0.06em;
    --ccg-header-menu-case: uppercase;

    position: sticky;
    top: 0;
    z-index: 40;
    background: var(--ccg-cream);
    border-bottom: 1px solid var(--ccg-ink);
    font-family: 'Karla', system-ui, sans-serif;
  }

  .ccg-header__bar {
    max-width: 1280px;
    margin-inline: auto;
    padding-inline: 24px;
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 24px;
    min-height: 78px;
  }

  .ccg-header__logo {
    display: flex;
    align-items: center;
    gap: 8px;
    text-decoration: none;
    color: var(--ccg-ink);
  }

  .ccg-header__logo-icon {
    width: 32px;
    height: 20px;
    flex-shrink: 0;
  }

  .ccg-header__logo-img {
    display: block;
    height: 36px;
    width: auto;
  }

  .ccg-header__logo-text {
    font-family: 'Rye', serif;
    font-size: 16px;
    line-height: 1.2;
    letter-spacing: 0.02em;
  }

  .ccg-header__nav {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 28px;
  }

  .ccg-header__nav-link {
    font-family: var(--ccg-header-menu-font-family);
    font-size: 15px;
    font-weight: var(--ccg-header-menu-font-weight);
    letter-spacing: var(--ccg-header-menu-letter-spacing);
    text-transform: var(--ccg-header-menu-case);
    color: var(--ccg-ink);
    text-decoration: none;
    text-underline-offset: 4px;
    padding-bottom: 4px;
    border-bottom: 1px solid transparent;
  }

  .ccg-header__nav-link:hover {
    border-bottom-color: var(--ccg-tan);
    text-decoration: underline;
  }

  .ccg-header__nav-link--active {
    border-bottom-color: var(--ccg-ink);
  }

  .ccg-header__actions {
    display: flex;
    align-items: center;
    gap: 20px;
  }

  .ccg-header__action {
    position: relative;
    display: flex;
    align-items: center;
    font-family: var(--ccg-header-menu-font-family);
    font-size: 14px;
    font-weight: var(--ccg-header-menu-font-weight);
    letter-spacing: var(--ccg-header-menu-letter-spacing);
    text-transform: var(--ccg-header-menu-case);
    color: var(--ccg-ink);
    text-decoration: none;
    text-underline-offset: 4px;
    white-space: nowrap;
  }

  .ccg-header__action:hover {
    color: var(--ccg-tan);
    text-decoration: underline;
  }

  .ccg-header__action-icon {
    display: none;
    width: 22px;
    height: 22px;
  }

  .ccg-header__cart-count {
    display: none;
  }

  .ccg-header__mobile-toggle {
    display: none;
  }

  .ccg-header__mobile-summary {
    list-style: none;
    cursor: pointer;
    width: 28px;
    height: 22px;
    display: flex;
    align-items: center;
  }

  .ccg-header__mobile-summary::-webkit-details-marker {
    display: none;
  }

  .ccg-header__burger,
  .ccg-header__burger::before,
  .ccg-header__burger::after {
    display: block;
    width: 100%;
    height: 1px;
    background: var(--ccg-ink);
    position: relative;
  }

  .ccg-header__burger::before,
  .ccg-header__burger::after {
    content: '';
    position: absolute;
    left: 0;
  }

  .ccg-header__burger::before {
    top: -7px;
  }

  .ccg-header__burger::after {
    top: 7px;
  }

  .ccg-header__mobile-nav {
    display: flex;
    flex-direction: column;
    gap: 16px;
    padding: 20px 24px 28px;
    border-top: 1px solid var(--ccg-ink);
  }

  .ccg-header__mobile-link {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 16px;
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--ccg-ink);
    text-decoration: none;
  }

  .ccg-header__mobile-actions {
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin-top: 8px;
    padding-top: 16px;
    border-top: 1px solid rgb(33 28 23 / 0.15);
  }

  @media screen and (max-width: 989px) {
    .ccg-header__nav {
      display: none;
    }
  }

  @media screen and (max-width: 749px) {
    .ccg-header__bar {
      grid-template-columns: auto 1fr auto;
      min-height: 64px;
    }

    .ccg-header__actions {
      gap: 16px;
      order: 3;
    }

    .ccg-header__action-icon {
      display: block;
    }

    .ccg-header__action-text {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }

    .ccg-header__action--cart {
      position: relative;
    }

    .ccg-header__cart-count {
      display: flex;
      align-items: center;
      justify-content: center;
      position: absolute;
      top: -6px;
      right: -8px;
      min-width: 16px;
      height: 16px;
      padding: 0 3px;
      border-radius: 999px;
      background: var(--ccg-ink);
      color: var(--ccg-cream);
      font-family: 'Barlow Condensed', sans-serif;
      font-size: 10px;
      line-height: 1;
    }

    .ccg-header__mobile-toggle {
      display: block;
      justify-self: start;
      order: 1;
    }

    .ccg-header__logo {
      order: 2;
      justify-self: center;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "CCG Header",
  "tag": "section",
  "class": "section-wrapper",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo_image",
      "label": "Logo image",
      "info": "Leave blank to use the bison icon with the shop name."
    },
    {
      "type": "text",
      "id": "logo_text",
      "label": "Logo text",
      "default": "Canyon Creek Goods"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "default": "header-menu"
    }
  ],
  "presets": [
    {
      "name": "CCG Header"
    }
  ]
}
{% endschema %}
